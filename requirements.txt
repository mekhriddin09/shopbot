aiogram==3.31.0
SQLAlchemy==2.0.36
greenlet==3.1.1
aiosqlite==0.20.0
alembic==1.14.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1
httpx==0.27.2
tenacity==9.0.0

# Telegram Stars / Premium delivery via Fragment.com.
# PINNED ON PURPOSE: this library reverse-engineers fragment.com (12 major
# versions in its first year) and the wallet seed passes through it, so the
# version is only ever changed deliberately, after re-reading the diff.
fragment-api-py==12.1.0
